#
temp=[32.5,31.8,29.5,30.2,32.0]
total=0
for t in temp:
    total+=t
av=print(total/len(temp))
print(av)

#creator of numpy is travis oliphant


import numpy as np

temp=np.array([32.5,31.8,29.5,30.2,32.0])
av=np.mean(temp)
print(av)

#array is diff from list
#array is more efficient
#array is faster
#array is more compact

#numpy array is faster and more compact than list
#easy math operations
#easy indexing and slicing
#many built in functions

#################################################

